<%= form_for(@privacy_policy_top_text) do |f| %>
  <% if @privacy_policy_top_text.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@privacy_policy_top_text.errors.count, "error") %> prohibited this privacy_policy_top_text from being saved:</h2>

      <ul>
      <% @privacy_policy_top_text.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :content %><br>
    <%= f.text_area :content,class: "form-control" %>
  </div>
  <div class="actions">
    <%= f.submit class: "btn btn-success" %>
  </div>
<% end %>
